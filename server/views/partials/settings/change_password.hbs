<section id="change-password-wrapper">
  <h2>
    Mainīt paroli
  </h2>
  <p>Ievadi pašreizējo paroli un jauno paroli, uz kuru vēlies to nomainīt.</p>
  <form 
    id="change-password"
    hx-post="/api/auth/change-password"
    hx-select="form"
    hx-swap="outerHTML"
    hx-sync="this:abort"
  >
    <div class="inputs">
      <label class="{{#if errors.currentpassword}}error{{/if}}">
        Pašreizējā parole:
        <input 
          id="currentpassword" 
          name="currentpassword" 
          type="password" 
          placeholder="Pašreizējā parole..."
          hx-preserve="true"
        />
        {{#if errors.currentpassword}}<p class="error">{{errors.currentpassword}}</p>{{/if}}
      </label>
      <label class="{{#if errors.newpassword}}error{{/if}}">
        Jaunā parole:
        <input 
          id="newpassword" 
          name="newpassword" 
          type="password" 
          placeholder="Jaunā parole..."
          hx-preserve="true"
          autocomplete="new-password"
        />
        {{#if errors.newpassword}}<p class="error">{{errors.newpassword}}</p>{{/if}}
      </label>
    </div>
    <button type="submit" class="primary">
      <span>{{> icons/reload}}</span>
      <span>{{> icons/spinner}}</span>
      Atjaunināt
    </button>
    {{#if error}}
      {{#unless errors}}
        <p class="error">{{error}}</p>
      {{/unless}}
    {{else if success}}
      <p class="success">{{success}}</p>
    {{/if}}
  </form>
</section>