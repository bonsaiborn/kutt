<div class="content admin-create">
  <h2>Pievienot domēnu</h2>
  <form
    id="add-domain-form"
    hx-post="/api/domains/admin" 
    hx-target="closest .content" 
    hx-swap="outerHTML" 
    hx-indicator="closest .content"
  >
    <label class="{{#if errors.address}}error{{/if}}">
      Adrese:
      <input
        name="address"
        id="add-domain-address"
        type="text"
        placeholder="Adrese..."
        hx-preserve="true"
      />
      {{#if errors.address}}<p class="error">{{errors.address}}</p>{{/if}}
    </label>
    <label class="{{#if errors.homepage}}error{{/if}}">
      Sākumlapa (nav obligāti):
      <input
        name="homepage"
        id="add-domain-homepage"
        type="text"
        placeholder="Sākumlapas adrese..."
        hx-preserve="true"
      />
      {{#if errors.homepage}}<p class="error">{{errors.homepage}}</p>{{/if}}
    </label>
    <label class="checkbox">
      <input 
        id="add-domain-banned" 
        name="banned"
        type="checkbox"
        onchange="canSendVerificationEmail();" 
        hx-preserve="true"
      />
      Bloķēts
    </label>
    <div class="buttons">
      <button type="button" hx-on:click="closeDialog()">Atcelt</button>
      <button type="submit" class="primary">
        <span>{{> icons/plus}}</span>
        Pievienot
      </button>
      {{> icons/spinner}}
    </div>
  </form>
  <div id="dialog-error">
    {{#if error}}
      <p class="error">{{error}}</p>
    {{/if}}
  </div>
</div>